import React from "react";

import {
  StyleSheet,
  FlatList
} from 'react-native';

export default class SuperGridList extends React.Component {
  constructor(props) {
    super(props);
    this.state = {
    };
  }

  render() {
    return null;
  }
}

const styles = StyleSheet.create({
});
