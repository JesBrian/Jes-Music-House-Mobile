import React from "react";

import {
  StyleSheet
} from 'react-native';

export default class SuperFlatList extends React.Component {
  constructor(props) {
    super(props);
    this.state = {
    };
  }

  render() {
    return null;
  }
}

const styles = StyleSheet.create({
});
